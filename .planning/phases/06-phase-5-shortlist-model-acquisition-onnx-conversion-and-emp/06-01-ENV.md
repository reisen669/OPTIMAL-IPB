# Phase 6 Plan 01 — Environment Setup Record

**Date:** 2026-06-24
**Task:** 06-01-01 — Install torch + ultralytics into qgis_gdal_env

## Installed Packages

Installed into `C:\Users\suily\miniconda3\envs\qgis_gdal_env` using the conda env Python executable:

| Package | Version | Install Command |
|---------|---------|----------------|
| torch | 2.12.1+cpu | `pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu` |
| torchvision | 0.27.1+cpu | (same command as torch) |
| ultralytics | 8.4.75 | `pip install ultralytics` |
| opencv-python | 4.13.0.92 | force-reinstall to fix broken cv2 module (was missing __init__.py) |

## Deviation

opencv-python 4.13.0.92 was already installed but had a broken `cv2` module (namespace package
with no `__init__.py` — the actual `.pyd` binary was in `~v2/` directory). This caused
`ultralytics` to fail with `AttributeError: module 'cv2' has no attribute 'imshow'`.
Force-reinstalled opencv-python to fix the `cv2` package layout.

## Verification

```
C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -c "import torch; print(torch.__version__)"
# => 2.12.1+cpu

C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -c "import ultralytics; print(ultralytics.__version__)"
# => 8.4.75

C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -c "import torchvision; print(torchvision.__version__)"
# => 0.27.1+cpu
```

All three commands exit 0 and print version strings.
