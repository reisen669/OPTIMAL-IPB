---
phase: 06-phase-5-shortlist-model-acquisition-onnx-conversion-and-emp
plan: 06-02
title: "Wave 1 — E1 CanopyRS: verify weight download URL, install canopyrs, test Python 3.12 compatibility"
wave: 1
depends_on: []
files_modified:
  - download_missing.py
autonomous: false
requirements: [VHR-02, VHR-03]

must_haves:
  truths:
    - "E1 CanopyRS weight download URL accessibility confirmed (via manual browser check)"
    - "If weights accessible: canopyrs package installed or dependencies documented"
    - "If weights accessible: Python 3.12 import tested (import canopyrs or import torch on checkpoint)"
    - "If URL or import fails: blocker documented with specific error message for 06-TEST-RESULTS.md"
  artifacts:
    - path: "models/canopyrs_frcnn_r50.pth"
      provides: "E1 CanopyRS Faster R-CNN+R50 checkpoint (if download succeeds)"
      min_lines: 0
    - path: "download_missing.py"
      provides: "Updated download script with E1 URL entry (or blocker note)"
  key_links:
    - from: "browser → GitHub Releases page"
      to: "models/canopyrs_frcnn_r50.pth"
      via: "manual download or documented blocker URL"
      pattern: "hugobaudchon/CanopyRS/releases"
    - from: "qgis_gdal_env Python 3.12"
      to: "torchvision.models.detection.fasterrcnn_resnet50_fpn"
      via: "import test (conditional on weight download success)"
      pattern: "fasterrcnn_resnet50_fpn"
---

<objective>
Verify whether the E1 CanopyRS pretrained weights are downloadable from the GitHub Releases page, install the canopyrs package (or its dependencies), and test Python 3.12 compatibility of the Faster R-CNN+R50 preset. If the weight URL is inaccessible or Python 3.12 compatibility fails, document the blocker clearly — E1 tests in Wave 2 (Plan 06-03) are gated on this plan's success.

Purpose: E1 CanopyRS is conditionally the best candidate for the 3-10 cm/px tier (tropical rainforest training, GSD matches Perak 5cm and Rupat 8.8cm). However, the weight download URL was UNCONFIRMED by WebFetch during Phase 5 research. This plan resolves that ambiguity before committing to full ONNX export and empirical testing.

Output: A definite answer on weight accessibility (yes/no), a canopyrs installation (or dependency checklist), and a Python 3.12 compatibility verdict. Results go into 06-TEST-RESULTS.md by Plan 06-04.
</objective>

<execution_context>
@C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/.planning/ROADMAP.md
@C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/.planning/phases/06-phase-5-shortlist-model-acquisition-onnx-conversion-and-emp/06-RESEARCH.md (E1 CanopyRS section — repository URL, architecture presets, download path, ONNX export options)
@C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/.planning/phases/05-extended-palm-detection-model-research/05-EXTENDED-REPORT.md (E1 CanopyRS shortlist entry — conditional status, weight URL unconfirmed, Faster R-CNN+R50 recommended)
@C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/download_missing.py
@C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/__init__.py

<interfaces>
<!-- From 06-RESEARCH.md — E1 CanopyRS details -->

Repository: https://github.com/hugobaudchon/CanopyRS
Paper: arXiv:2507.00170 (XPRIZE Rainforest)
Architecture presets:
  - DINO + Swin-L 384 (primary — complex ONNX export)
  - Faster R-CNN + R50 (alternative — standard torchvision, easier export)
  - DINO + R50 (alternative)
GSD: 3–10 cm/px (covers Perak 5cm, Rupat 8.8cm)
License: Apache-2.0
Weight download: GitHub Releases (https://github.com/hugobaudchon/CanopyRS/releases) — UNCONFIRMED

Target Python 3.12 compatibility test (per 06-RESEARCH.md):
```
from torchvision.models.detection import fasterrcnn_resnet50_fpn
```
Standard torchvision — expected to work on Python 3.12 if torchvision is installed.

<!-- From 05-EXTENDED-REPORT.md — Phase 6 action for E1 -->
"(1) Verify weight download URL by visiting https://github.com/hugobaudchon/CanopyRS/releases in browser;
 (2) Install canopyrs package;
 (3) Confirm Python 3.12 compatibility;
 (4) Test Faster R-CNN+R50 preset on Perak 5cm raster;
 (5) Attempt ONNX export of detection preset."

<!-- qgis_gdal_env package state (confirmed 2026-06-19) -->
- torch: NOT INSTALLED (will be installed by 06-01 which runs in parallel in Wave 1)
- torchvision: NOT INSTALLED (installed together with torch in 06-01)
- ultralytics: NOT INSTALLED (installed in 06-01)
- onnxruntime 1.17.0: INSTALLED
- numpy 2.2.6: INSTALLED
- opencv-python 4.13.0.92: INSTALLED

IMPORTANT: Plan 06-01 also runs in Wave 1 and installs torch + torchvision. If 06-01 completes first, torchvision is available for E1 import testing. If 06-02 runs first, it may need to install torchvision itself (omitted from this plan to avoid conflict — both plans touching qgis_gdal_env is safe since pip installs are idempotent).
</interfaces>
</context>

<tasks>

<task id="06-02-01" type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Manual browser verification — check E1 CanopyRS GitHub Releases for downloadable weights</name>
  <read_first>
    - C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/.planning/phases/06-phase-5-shortlist-model-acquisition-onnx-conversion-and-emp/06-RESEARCH.md (E1 CanopyRS section — GitHub Releases URL and paper's claim about public weights)
  </read_first>
  <what-built>Claude has prepared the Phase 5 research but was unable to verify the E1 weight URL via automated WebFetch (GitHub Release assets page failed to load). This is a human-only action because the GitHub Releases page may be behind authentication or a browser-based captcha.</what-built>
  <how-to-verify>
**Step 1:** Open a web browser and visit:
https://github.com/hugobaudchon/CanopyRS/releases

**Step 2:** Look for release assets (attached files at the bottom of a release page). The paper (arXiv:2507.00170) states: "Our dataset, code, and pre-trained weights are made public."

**Step 3:** Check for weight files with any of these patterns:
- `*.pth` (PyTorch checkpoint)
- `*.pt` (PyTorch model)
- `*.ckpt` (lightning checkpoint)
- `faster_rcnn` or `frcnn` in filename
- `dino` or `swin` in filename
- `canopyrs` in filename

**Step 4:** If weights are present, note:
- The exact filenames and their sizes
- Whether there are separate files for different architecture presets (Faster R-CNN+R50 vs DINO+Swin-L)
- Whether there are model config/TOML files accompanying the weights

**Step 5:** If NO release exists or no weights attached, check:
- The README.md at https://github.com/hugobaudchon/CanopyRS for download instructions
- Whether there is a separate HuggingFace or Zenodo repository linked in the README
- Whether there is a `weights/` or `checkpoints/` directory in the repository itself
  </how-to-verify>
  <resume-signal>Respond with one of:
- "weights found: [filenames and sizes]" — proceed to Task 2
- "no weights: [what you found / didn't find]" — Task 2 will document the blocker
- "partial: [details]" — describe what is available</resume-signal>
</task>

<task id="06-02-02" type="auto">
  <name>Task 2: Download E1 weights (if available) or document blocker; install canopyrs dependencies; test Python 3.12 import</name>
  <read_first>
    - C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/.planning/phases/06-phase-5-shortlist-model-acquisition-onnx-conversion-and-emp/06-RESEARCH.md (E1 CanopyRS section — Faster R-CNN+R50 ONNX export path, Python 3.12 compatibility assessment)
    - C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/download_missing.py (existing download script pattern)
    - C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/optimal_ipb_algorithm.py (lines 64-71 — sys.path imports for onnxruntime, keras_retinanet, helpers)
    - C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/.planning/phases/05-extended-palm-detection-model-research/05-EXTENDED-REPORT.md (E1 CanopyRS Phase 6 action items 1-4)
  </read_first>
  <action>
**Part A — Act on the verification result from Task 1:**

If the user reported **"weights found"** with filenames:
1. Download the Faster R-CNN+R50 checkpoint (.pth file) from the GitHub Releases page to:
   `C:\Users\suily\AppData\Roaming\QGIS\QGIS3\profiles\default\python\plugins\optimal-ipb\models\canopyrs_frcnn_r50.pth`
2. If the release also has a model config file (e.g., `frcnn_r50.toml`), download it to:
   `C:\Users\suily\AppData\Roaming\QGIS\QGIS3\profiles\default\python\plugins\optimal-ipb\models\canopyrs_frcnn_r50.toml`
3. Document the download URL and filename in `download_missing.py` by adding the E1 entry alongside existing model entries.

If the user reported **"no weights"**:
1. Do NOT create any model files.
2. Add a comment block in `download_missing.py` documenting the blocker:
   ```python
   # E1 CanopyRS (hugobaudchon/CanopyRS): no pretrained weights found on GitHub Releases as of 2026-06-19.
   # Paper (arXiv:2507.00170) claims public weights but release page is empty.
   # URL checked: https://github.com/hugobaudchon/CanopyRS/releases
   # Status: BLOCKED — Phase 6 E1 empirical testing skipped.
   ```

**Part B — Install canopyrs dependencies (or verify what's needed):**

First, check if the canopyrs package itself is installable:
```
C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -m pip install canopyrs 2>&1
```

If canopyrs installs successfully:
- Document the version installed
- Run: `C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -c "import canopyrs; print(dir(canopyrs))"` to list available modules

If canopyrs fails to install (expected — it may have no PyPI package), install the core dependencies individually:
```
C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -m pip install transformers timm pycocotools
```

Note: If 06-01 already installed torch/torchvision, the pip installs above will be no-ops. Both plans touching the same pip environment is safe.

**Part C — Test Python 3.12 compatibility:**

Run ALL THREE of these import tests:
1. Faster R-CNN + R50 (primary E1 export preset):
```python
C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -c "from torchvision.models.detection import fasterrcnn_resnet50_fpn; print('fasterrcnn_resnet50_fpn: OK')"
```

2. (Optional) DINO + Swin-L (complex, only if weights include this preset):
```python
C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -c "import torch; print('torch version:', torch.__version__); print('cuda available:', torch.cuda.is_available())"
```

3. ONNX export capability:
```python
C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -c "import torch.onnx; print('torch.onnx: OK')"
```

For each test, note:
- Exit code (0 = success, 1 = failure)
- Error message if any (especially import errors for transformers, timm, or specific torch versions)
- Whether DINO + Swin-L dependencies are satisfied (if attempted)

If ANY of the three critical imports (fasterrcnn_resnet50_fpn, torch.onnx) fails:
- Document the full error in a file `./planning/phases/06-phase-5-shortlist-model-acquisition-onnx-conversion-and-emp/e1_compatibility_notes.txt`
- This IS a blocker for Plan 06-03 ONNX export

**Part D — Summarize findings:**

Write a summary to stdout with this exact format for use by Plan 06-04:
```
E1 CANOPYRS COMPATIBILITY REPORT
================================
Weight URL: [ACCESSIBLE / BLOCKED]
Weight file: [path or "N/A"]
canopyrs package: [INSTALLED vX.Y.Z / NOT INSTALLABLE / NOT NEEDED]
fasterrcnn_resnet50_fpn import: [PASS / FAIL]
torch.onnx import: [PASS / FAIL]
Python 3.12 overall: [COMPATIBLE / INCOMPATIBLE]
Recommendation for Plan 06-03: [PROCEED / BLOCKED — reason]
```
  </action>
  <acceptance_criteria>
    - If weights found: `test -f "C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/models/canopyrs_frcnn_r50.pth"` exits 0
    - If weights NOT found: `grep -c "E1 CanopyRS.*BLOCKED" C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/download_missing.py` returns at least 1
    - `C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -c "from torchvision.models.detection import fasterrcnn_resnet50_fpn; print('OK')"` either exits 0 (and prints "OK") or the exit code and error are documented
    - `C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -c "import torch.onnx; print('OK')"` either exits 0 or the exit code and error are documented
    - The stdout from the end of execution contains the exact "E1 CANOPYRS COMPATIBILITY REPORT" header
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GitHub Releases | Manual human action; GitHub is trusted infrastructure |
| GitHub Releases → local models/ | Downloaded checkpoint binary; verify file size is reasonable for a detection model (50-500 MB) |
| PyPI → qgis_gdal_env | Pip install of torchvision, transformers, timm — standard PyPI packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | GitHub Releases URL | accept | URL is public; no credentials involved |
| T-06-05 | Elevation of Privilege | pip install canopyrs | accept | canopyrs is from PyPI; if it fails, manual dependency install uses well-known packages |
| T-06-06 | Denial of Service | weight download | accept | If weights unavailable, document blocker and skip E1 testing — no escalation needed |
</threat_model>

<verification>
Run these checks after Task 2 completes:

1. `test -f "models/canopyrs_frcnn_r50.pth"` — weight file exists (if downloaded) OR documented as absent
2. `grep -c "E1 CanopyRS" download_missing.py` — at least 1 reference (either download entry or blocker comment)
3. `C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -c "from torchvision.models.detection import fasterrcnn_resnet50_fpn; print('OK')"` — exits 0 with "OK" (or failure documented)
4. `C:/Users/suily/miniconda3/envs/qgis_gdal_env/python.exe -c "import torch.onnx; print('OK')"` — exits 0 with "OK" (or failure documented)
5. E1 CANOPYRS COMPATIBILITY REPORT block in stdout with all 7 lines filled
</verification>

<success_criteria>
E1 CanopyRS verification complete when:
- The weight download URL has been checked in a browser (manual checkpoint done)
- The weight accessibility status is definitively confirmed (accessible or blocked) and documented
- If weights accessible: .pth file downloaded to models/
- Python 3.12 import tests for torchvision and torch.onnx completed and results recorded
- E1 CANOPYRS COMPATIBILITY REPORT produced with clear recommendation for Plan 06-03 (PROCEED or BLOCKED)
- All findings documented for 06-TEST-RESULTS.md assembly in Plan 06-04
</success_criteria>

<output>
After completion, create `.planning/phases/06-phase-5-shortlist-model-acquisition-onnx-conversion-and-emp/06-02-SUMMARY.md` using the summary template.
</output>
