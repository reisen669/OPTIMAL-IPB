---
phase: 01-qgis-312-compat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - PLUG-05
must_haves:
  truths:
    - "onnxruntime can be imported by the Python interpreter inside qgis_gdal_env"
    - "import onnxruntime succeeds without error"
  artifacts:
    - path: "C:\\Users\\suily\\miniconda3\\envs\\qgis_gdal_env\\Lib\\site-packages\\onnxruntime"
      provides: "onnxruntime package installed in the injected conda env"
  key_links:
    - from: "__init__.py sys.path injection"
      to: "qgis_gdal_env\\Lib\\site-packages"
      via: "sys.path.insert(0, _site_pkgs)"
      pattern: "sys\\.path\\.insert"
---

<objective>
Install onnxruntime into the qgis_gdal_env conda environment so the dependency injected by __init__.py exposes it to the QGIS plugin at runtime.

Purpose: Every subsequent inference task depends on `import onnxruntime as ort` succeeding. __init__.py injects `C:\Users\suily\miniconda3\envs\qgis_gdal_env\Lib\site-packages` into sys.path at plugin load time, so installing onnxruntime into that environment is the only step required — no QGIS or plugin code changes are needed.

Output: onnxruntime package present in qgis_gdal_env, importable via the injected sys.path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-qgis-312-compat/01-CONTEXT.md

<interfaces>
<!-- Conda env path from __init__.py (already committed) -->
<!-- _CONDA_ENV = r"C:\Users\suily\miniconda3\envs\qgis_gdal_env" -->
<!-- _site_pkgs = os.path.join(_CONDA_ENV, "Lib", "site-packages") -->
<!-- sys.path.insert(0, _site_pkgs)  — line 37 of __init__.py -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install onnxruntime via conda into qgis_gdal_env</name>
  <files>C:\Users\suily\miniconda3\envs\qgis_gdal_env\Lib\site-packages\onnxruntime</files>

  <read_first>
    - C:\Users\suily\AppData\Roaming\QGIS\QGIS3\profiles\default\python\plugins\optimal-ipb\__init__.py
      (confirm _CONDA_ENV path and site-packages injection before running install)
  </read_first>

  <action>
Run the following command in PowerShell (per D-06). The `-y` flag avoids interactive prompts:

```powershell
conda install -n qgis_gdal_env onnxruntime -y
```

If conda is not on PATH, use the full path:

```powershell
& "$env:USERPROFILE\miniconda3\Scripts\conda.exe" install -n qgis_gdal_env onnxruntime -y
```

After installation completes, verify the import works using the qgis_gdal_env Python interpreter directly:

```powershell
& "C:\Users\suily\miniconda3\envs\qgis_gdal_env\python.exe" -c "import onnxruntime; print(onnxruntime.__version__)"
```

Expected: prints a version string (e.g. `1.20.1`) with no error output.

Do NOT install onnxruntime-gpu — the CONTEXT.md (Deferred Ideas) explicitly defers GPU inference (CUDAExecutionProvider). CPU-only onnxruntime is the correct package.
  </action>

  <verify>
    <automated>
      & "C:\Users\suily\miniconda3\envs\qgis_gdal_env\python.exe" -c "import onnxruntime as ort; print('ort version:', ort.__version__)"
    </automated>
  </verify>

  <acceptance_criteria>
    - Command exits with code 0
    - Output contains "ort version:" followed by a version string
    - No ImportError or ModuleNotFoundError in output
    - Directory exists: C:\Users\suily\miniconda3\envs\qgis_gdal_env\Lib\site-packages\onnxruntime
  </acceptance_criteria>

  <done>onnxruntime is importable from qgis_gdal_env's Python interpreter with no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conda package registry → local env | Package downloaded from conda-forge/defaults; no user code involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | conda package install | accept | onnxruntime is a Microsoft-maintained package on conda-forge; SHA verification is handled by conda's package manager |
| T-01-02 | Denial of Service | conda solver version conflicts | mitigate | If solver conflicts arise, pin with `onnxruntime=1.*` to avoid pulling an incompatible version |
</threat_model>

<verification>
Confirm onnxruntime is installed and importable:

```powershell
& "C:\Users\suily\miniconda3\envs\qgis_gdal_env\python.exe" -c "import onnxruntime as ort; print('ort version:', ort.__version__)"
```

Must print a version string. No traceback acceptable.
</verification>

<success_criteria>
- `conda install -n qgis_gdal_env onnxruntime -y` completes without error
- `python.exe -c "import onnxruntime"` in qgis_gdal_env exits 0
- PLUG-05 satisfied: qgis_gdal_env has onnxruntime installed
</success_criteria>

<output>
After completion, create `.planning/phases/01-qgis-312-compat/01-01-SUMMARY.md` documenting:
- onnxruntime version installed
- Any solver conflicts encountered and resolved
- Confirmation of import test output
</output>
