---
phase: 04-palm-model-download-onnx-conversion-and-empirical-testing-on
plan: 04-02
title: "Wave 1 — Verify all ONNX models with onnxruntime and record tensor names in 04-TEST-RESULTS.md"
wave: 1
depends_on: [04-01]
files_modified:
  - .planning/phases/04-palm-model-download-onnx-conversion-and-empirical-testing-on/04-TEST-RESULTS.md
autonomous: false
requirements: [MOD-02]

must_haves:
  truths:
    - "All six ONNX model files (5 root + MOPAD) have been loaded by onnxruntime and their result ([OK] or [FAIL]) recorded"
    - "Tensor input/output names for every [OK] model are written into 04-TEST-RESULTS.md"
    - "MOPAD output names (expected: dets, labels) are confirmed or discrepancy noted"
    - "Geoeye-Resnet101.onnx and Pleiades-Resnet101.onnx verification result is recorded; if [FAIL] they are marked excluded for testing"
  artifacts:
    - path: ".planning/phases/04-palm-model-download-onnx-conversion-and-empirical-testing-on/04-TEST-RESULTS.md"
      provides: "Completed ONNX Verification table with Verify Result, Input Names, Output Names for all 6 models"
  key_links:
    - from: "verify_onnx_models.py"
      to: "models/ and models/mopad/"
      via: "glob.glob recursive discovery"
      pattern: "recursive=True"
    - from: "04-TEST-RESULTS.md verification table"
      to: "Wave 2/3 test plan"
      via: "B3/B4 test rows skipped if Geoeye/Pleiades verify [FAIL]"
---

<objective>
Run the patched `verify_onnx_models.py` script against all six ONNX model files on disk and record each model's load result, tensor names, and any errors in 04-TEST-RESULTS.md. This is a checkpoint — the human runs the script and fills in the results.

Purpose: Confirm which models are loadable before spending 5-15 minutes per test run. Catch B3/B4 uncertainty (per D-02) before Wave 3. Confirm MOPAD output tensor names ('dets'/'labels') before Wave 2 testing.

Output: Completed ONNX Verification section of 04-TEST-RESULTS.md. Geoeye/Pleiades marked [OK] or [FAIL] to gate Wave 3.
</objective>

<execution_context>
@C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/.planning/phases/04-palm-model-download-onnx-conversion-and-empirical-testing-on/04-CONTEXT.md
@C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/.planning/phases/04-palm-model-download-onnx-conversion-and-empirical-testing-on/04-RESEARCH.md

<interfaces>
<!-- Expected verify_onnx_models.py output format (after Wave 0 patch) -->
<!-- The script prints relative paths including the mopad/ subdirectory: -->
<!--
Models directory: C:\...\optimal-ipb\models
ONNX files found: ['models/Geoeye-Resnet101.onnx', 'models/Google-Resnet101.onnx',
                   'models/Pleiades-Resnet101.onnx', 'models/tree_tops_yolov9.onnx',
                   'models/tribber93_yolov11_palm.onnx', 'models/mopad/MOPAD_epoch_24.onnx']

  [OK]  models/Google-Resnet101.onnx (211 MB)
        inputs=['input_1']
        outputs=['filtered_detections/1', 'filtered_detections/2', 'filtered_detections/3']

  [OK]  models/mopad/MOPAD_epoch_24.onnx (242 MB)
        inputs=['image']
        outputs=['dets', 'labels']          <-- confirm these exact names

  [FAIL] models/Geoeye-Resnet101.onnx (221 MB): <error details>   <-- possible
-->

<!-- 04-TEST-RESULTS.md verification table row format to fill in: -->
<!--
| models/mopad/MOPAD_epoch_24.onnx | 242 | [OK] | ['image'] | ['dets','labels'] | confirmed |
| models/Geoeye-Resnet101.onnx | 221 | [FAIL] | — | — | excluded per D-02 |
-->
</interfaces>
</context>

<tasks>

<task id="04-02-01" type="checkpoint:human-verify">
  <name>Task 1: Run verify_onnx_models.py from QGIS Python Console and capture full output</name>
  <read_first>
    - C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/verify_onnx_models.py (confirm patch is in place — should contain 'recursive=True' before running)
  </read_first>
  <action>
**Pre-flight check:** Confirm the patch from Wave 0 is in place:
```
grep -c "recursive=True" verify_onnx_models.py
```
Must return `1`. If `0`, Wave 0 task 2 was not completed — stop and fix.

**Run the script:**

Open QGIS 3.44.2. Open the Python Console (Ctrl+Alt+P). Paste and run:

```python
plugin_dir = r'C:\Users\suily\AppData\Roaming\QGIS\QGIS3\profiles\default\python\plugins\optimal-ipb'
exec(open(plugin_dir + r'\verify_onnx_models.py').read())
```

**Alternative (if exec gives path issues):**
```python
import runpy, sys
plugin_dir = r'C:\Users\suily\AppData\Roaming\QGIS\QGIS3\profiles\default\python\plugins\optimal-ipb'
sys.path.insert(0, plugin_dir)
runpy.run_path(plugin_dir + r'\verify_onnx_models.py')
```

**Expected output structure:**
```
Models directory: C:\...\optimal-ipb\models
ONNX files found: ['models/Geoeye-Resnet101.onnx', 'models/Google-Resnet101.onnx',
                   'models/Pleiades-Resnet101.onnx', 'models/tree_tops_yolov9.onnx',
                   'models/tribber93_yolov11_palm.onnx', 'models/mopad/MOPAD_epoch_24.onnx']

  [OK]  models/Geoeye-Resnet101.onnx (221 MB)   ← OR [FAIL]
        inputs=[...]
        outputs=[...]
  [OK]  models/Google-Resnet101.onnx (211 MB)
        inputs=['input_1']
        outputs=['filtered_detections/1', 'filtered_detections/2', 'filtered_detections/3']
  ... (all 6 models)

All 6 ONNX models verified successfully.  ← OR "Some models failed verification."
```

Note: Loading all 6 models (especially 242 MB MOPAD) may take 30-90 seconds. QGIS Console may appear frozen — this is normal during ONNX session creation.

**Capture the full output** by copying the console text. You will need it for task 2.

**Important flags to note in the output:**
- Does `models/mopad/MOPAD_epoch_24.onnx` appear in the `ONNX files found` list? (It must — if missing, the recursive glob fix was not saved correctly.)
- For MOPAD outputs: are they `['dets', 'labels']`? If different names, note them — they must match what `mopad_algorithm.py` line 119 uses: `sess.run(['dets', 'labels'], {'image': blob})`
- For Geoeye and Pleiades: [OK] or [FAIL]?
  </action>
  <what-built>Wave 0 patched verify_onnx_models.py to use recursive glob. This task runs it and captures the output.</what-built>
  <how-to-verify>
    1. Open QGIS 3.44.2
    2. Open Python Console (Ctrl+Alt+P)
    3. Paste the exec() snippet above
    4. Wait for output to complete (up to 90 seconds)
    5. Verify the output lists exactly 6 models including `models/mopad/MOPAD_epoch_24.onnx`
    6. Note [OK] / [FAIL] for each model, and input/output tensor names
  </how-to-verify>
  <resume-signal>
Paste the full verify_onnx_models.py console output here. Include all [OK]/[FAIL] lines and tensor names. Type "done" if all 6 models passed, or list which models failed.
  </resume-signal>
</task>

<task id="04-02-02" type="auto">
  <name>Task 2: Record verify_onnx_models.py results in 04-TEST-RESULTS.md</name>
  <read_first>
    - C:/Users/suily/AppData/Roaming/QGIS/QGIS3/profiles/default/python/plugins/optimal-ipb/.planning/phases/04-palm-model-download-onnx-conversion-and-empirical-testing-on/04-TEST-RESULTS.md (current stub table)
  </read_first>
  <action>
Fill in the "ONNX Model Verification (MOD-02)" table in `04-TEST-RESULTS.md` based on the output captured in task 1. For each of the 6 model rows:

1. Set `Verify Result` to `[OK]` or `[FAIL]`
2. Set `Input Names` to the list shown (e.g., `['input_1']` or `['image']`)
3. Set `Output Names` to the list shown (e.g., `['filtered_detections/1', 'filtered_detections/2', 'filtered_detections/3']`)
4. Set `Notes` for special cases:
   - If MOPAD outputs are NOT `['dets', 'labels']`, note: "WARNING: mopad_algorithm.py line 119 expects ['dets','labels'] — output name mismatch will cause inference failure"
   - If Geoeye-Resnet101.onnx is [FAIL]: note "excluded per D-02"
   - If Pleiades-Resnet101.onnx is [FAIL]: note "excluded per D-02"

After filling the table, also update the Wave 3 plan rows (MR tests) to mark B3/B4 rows as "SKIPPED (verify failed)" if their respective models were [FAIL].

Commit the updated file:
```bash
git add ".planning/phases/04-palm-model-download-onnx-conversion-and-empirical-testing-on/04-TEST-RESULTS.md"
git commit -m "docs(04): record ONNX verification results (Wave 1)"
```
  </action>
  <acceptance_criteria>
    - `grep -c "\[OK\]\|\[FAIL\]" ".planning/phases/04-palm-model-download-onnx-conversion-and-empirical-testing-on/04-TEST-RESULTS.md"` returns 6 (one status per model)
    - `grep -c "MOPAD_epoch_24.onnx" ".planning/phases/04-palm-model-download-onnx-conversion-and-empirical-testing-on/04-TEST-RESULTS.md"` returns 1 (MOPAD row has a result)
    - `git log --oneline -1` shows a docs(04) commit with "Wave 1" in the message
  </acceptance_criteria>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -c "recursive=True" verify_onnx_models.py` returns 1 (pre-flight confirmed)
2. The verification table in 04-TEST-RESULTS.md has exactly 6 rows filled with [OK] or [FAIL]
3. MOPAD tensor names are noted — if not `['dets','labels']`, this is a blocking issue for Wave 2 MOPAD tests
4. Geoeye/Pleiades status is known — Wave 3 plan (04-04) runs B3/B4 only if [OK] here
</verification>

<success_criteria>
Wave 1 complete when:
- verify_onnx_models.py ran successfully and produced output for all 6 ONNX files
- 04-TEST-RESULTS.md verification table is fully filled with actual results
- MOPAD output tensor names are confirmed (or discrepancy flagged)
- B3/B4 gate is known: if Geoeye/Pleiades are [FAIL], Wave 3 04-04-PLAN.md skips those rows
</success_criteria>

<output>
After completion, create `.planning/phases/04-palm-model-download-onnx-conversion-and-empirical-testing-on/04-02-SUMMARY.md` using the summary template. Include the verify_onnx_models.py output and the pass/fail count.
</output>
